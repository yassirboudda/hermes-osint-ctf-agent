# Curated OSINT datasets (no secrets)

| File | Source | Notes |
|------|--------|-------|
| `maigret_subset.json` | [soxoj/maigret](https://github.com/soxoj/maigret) `data.json` (MIT) | Top Alexa-ranked sites without auth headers; nsfw/dating excluded. Soft-crawl default for `tools/maigret_cli.py`. |

Install the full Maigret package for the live 5900+ site DB: `pip install -r requirements-maigret.txt`.
